-- Compatibility marker for the Rustmix X4 dictionary SD pack.
-- Rustmix Wave uses a native Rust renderer and lookup engine, but it retains
-- the X4 APP.TOM / INDEX.TXT / DATA/*.JSN storage contract unchanged.
return { marker = "rustmix-lua-dictionary-pack-integrity-ok" }
